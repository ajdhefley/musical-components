export abstract class ExerciseBuilder {
    // protected difficulty: number;
    // protected timerPerProblem: number;
    // protected problems: any[];

    // protected abstract generateProblem(): any;

    // public withDifficulty(difficulty: number): ExerciseBuilder {
    //     this.difficulty = difficulty;
    //     return this;
    // }

    // public withTimerPerProblem(timer: number) {
    //     this.timerPerProblem = timer;
    //     // <= 0 means timer is off
    //     return this;
    // }

    // public withProblems(count: number): ExerciseBuilder {
    //     for (let i = 1; i <= count; i++)
    //         this.problems.push(this.generateProblem());
    //     return this;
    // }
}
