import './Header.scss';

function Header() {
    return (
        <header>
            <div className="logo" style={{ fontSize: '2rem' }}>Music App</div>
        </header>
    );
}

export default Header;
